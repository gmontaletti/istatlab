% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvd_metadata.R
\name{test_hvd_connectivity}
\alias{test_hvd_connectivity}
\title{Test HVD Endpoint Connectivity}
\usage{
test_hvd_connectivity(version = c("v1", "v2"), timeout = 30, verbose = TRUE)
}
\arguments{
\item{version}{Character vector specifying which API versions to test.
Valid values are \code{"v1"} and \code{"v2"}. Defaults to \code{c("v1", "v2")}, which
tests both.}

\item{timeout}{Numeric timeout in seconds for each individual connectivity
check. Default 30.}

\item{verbose}{Logical controlling structured logging and summary output.
Default TRUE.}
}
\value{
A data.frame with one row per tested endpoint and the following
columns:
\describe{
\item{version}{Character. API version (\code{"v1"} or \code{"v2"}).}
\item{endpoint}{Character. Endpoint type (\code{"data"} or \code{"structure"}).}
\item{url}{Character. Full URL that was tested.}
\item{accessible}{Logical. Whether the endpoint responded successfully.}
\item{status_code}{Integer. HTTP status code, or \code{NA} on connection
failure.}
\item{response_time}{Numeric. Round-trip time in seconds.}
\item{error_message}{Character. Error description, or empty string on
success.}
}
}
\description{
Performs lightweight HEAD-only requests against HVD API endpoints to verify
that the service is reachable. Tests both data and structure endpoints for
each requested API version.
}
\examples{
\dontrun{
# Test both v1 and v2 endpoints
status <- test_hvd_connectivity()

# Test only v1 endpoints
status <- test_hvd_connectivity(version = "v1")

# Test with a shorter timeout
status <- test_hvd_connectivity(timeout = 10, verbose = FALSE)
}
}
