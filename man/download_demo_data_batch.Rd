% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_download.R
\name{download_demo_data_batch}
\alias{download_demo_data_batch}
\title{Download Multiple Demographic Datasets}
\usage{
download_demo_data_batch(
  codes,
  year = NULL,
  territory = NULL,
  level = NULL,
  type = NULL,
  data_type = NULL,
  geo_level = NULL,
  subtype = NULL,
  cache_dir = NULL,
  force_download = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{codes}{Character vector of dataset codes to download.}

\item{year}{Integer year for the data file (patterns A, B, C, E).}

\item{territory}{Character string specifying geographic territory (Pattern B
only).}

\item{level}{Character string specifying geographic aggregation level
(Pattern C only).}

\item{type}{Character string specifying data completeness type (Pattern C
only).}

\item{data_type}{Character string specifying forecast data category
(Pattern D only).}

\item{geo_level}{Character string specifying geographic resolution
(Pattern D only).}

\item{subtype}{Character string specifying the data subtype (Pattern E only,
e.g., \code{"nascita"}, \code{"cittadinanza"}).}

\item{cache_dir}{Character string specifying directory for cached files.
If \code{NULL}, uses the config default.}

\item{force_download}{Logical indicating whether to bypass cache.
Default \code{FALSE}.}

\item{verbose}{Logical indicating whether to print status messages.
Default \code{TRUE}.}
}
\value{
A named list of \code{data.table} objects, one per dataset code.
Names correspond to the codes. Datasets that failed to download are
excluded from the list.
}
\description{
Downloads several different demographic datasets for the same set of
parameters. Each dataset is downloaded independently; failures are captured
as warnings and the remaining successful results are still returned.
}
\examples{
\dontrun{
# Download multiple demographic balance datasets
results <- download_demo_data_batch(c("D7B", "P02", "P03"), year = 2024)
}
}
