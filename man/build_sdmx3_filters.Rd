% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvd_config.R
\name{build_sdmx3_filters}
\alias{build_sdmx3_filters}
\title{Build SDMX 3.0 Dimension Filter Parameters}
\usage{
build_sdmx3_filters(start_time = NULL, end_time = NULL, dim_filters = NULL)
}
\arguments{
\item{start_time}{Character string specifying the start period (e.g.,
\code{"2020"}, \code{"2020-Q1"}, \code{"2020-01"}). Translated to a \verb{ge:} (greater or
equal) constraint on \code{TIME_PERIOD}. If \code{NULL}, no lower bound is applied.}

\item{end_time}{Character string specifying the end period. Translated to a
\verb{le:} (less or equal) constraint on \code{TIME_PERIOD}. If \code{NULL}, no upper
bound is applied.}

\item{dim_filters}{Named list where names are SDMX dimension identifiers and
values are the corresponding filter expressions. For example,
\code{list(FREQ = "M", REF_AREA = "IT")} produces \code{c[FREQ]=M} and
\code{c[REF_AREA]=IT}. Can be \code{NULL} for no dimension filtering.}
}
\value{
Character vector of query parameter strings suitable for appending
to a URL query string. Returns a zero-length character vector when no
filters are specified.
}
\description{
Translates time period boundaries and dimension constraints into the SDMX
3.0 query parameter syntax. Time periods are expressed as
\code{c[TIME_PERIOD]=ge:{start}+le:{end}} and arbitrary dimension filters as
\code{c[{DIM}]={value}}.
}
\examples{
\dontrun{
# Time range only
build_sdmx3_filters(start_time = "2020", end_time = "2025")
# "c[TIME_PERIOD]=ge:2020+le:2025"

# Open-ended time range (no end)
build_sdmx3_filters(start_time = "2020")
# "c[TIME_PERIOD]=ge:2020"

# Dimension filters only
build_sdmx3_filters(dim_filters = list(FREQ = "M", REF_AREA = "IT"))
# c("c[FREQ]=M", "c[REF_AREA]=IT")

# Combined time and dimension filters
build_sdmx3_filters(start_time = "2020", end_time = "2025",
                    dim_filters = list(FREQ = "M"))
# c("c[TIME_PERIOD]=ge:2020+le:2025", "c[FREQ]=M")

# No filters
build_sdmx3_filters()
# character(0)
}

}
\keyword{internal}
