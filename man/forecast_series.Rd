% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{forecast_series}
\alias{forecast_series}
\title{Forecast Time Series}
\usage{
forecast_series(
  data,
  value_col = "valore",
  time_col = "tempo",
  periods = 12,
  method = "auto.arima",
  confidence_levels = c(0.8, 0.95),
  group_vars = NULL
)
}
\arguments{
\item{data}{A data.table with time series data}

\item{value_col}{Character string specifying the value column name.
Default is "valore"}

\item{time_col}{Character string specifying the time column name.
Default is "tempo"}

\item{periods}{Integer number of periods to forecast. Default is 12}

\item{method}{Character string specifying forecasting method.
Options: "auto.arima", "ets", "naive", "linear". Default is "auto.arima"}

\item{confidence_levels}{Numeric vector of confidence levels for intervals.
Default is c(0.8, 0.95)}

\item{group_vars}{Character vector of variables to group by for forecasting}
}
\value{
A list containing forecast results and model information
}
\description{
Generates forecasts for ISTAT time series data using various methods.
}
\examples{
\dontrun{
# Generate 12-period forecast using auto.arima
forecast_results <- forecast_series(data, periods = 12)

# Forecast with different method
forecast_results <- forecast_series(data, method = "ets", periods = 6)

# Forecast by groups
forecast_results <- forecast_series(data, group_vars = c("region"))
}
}
