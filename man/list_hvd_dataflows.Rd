% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvd_metadata.R
\name{list_hvd_dataflows}
\alias{list_hvd_dataflows}
\title{List Available HVD Dataflows}
\usage{
list_hvd_dataflows(api_version = "hvd_v1", timeout = 120, verbose = TRUE)
}
\arguments{
\item{api_version}{Character string indicating the API surface to use.
One of \code{"hvd_v1"} (default) or \code{"hvd_v2"}.}

\item{timeout}{Numeric timeout in seconds for the HTTP request. Default 120.}

\item{verbose}{Logical controlling structured logging output. Default TRUE.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with columns \code{id}, \code{name}, \code{description},
and \code{agency}, or \code{NULL} if the request fails. A warning is issued on
failure.
}
\description{
Retrieves the catalogue of all dataflows published on the ISTAT Historical
Data Vault. Each dataflow corresponds to a downloadable dataset with its
own data structure definition.
}
\examples{
\dontrun{
# List all HVD dataflows using the v1 API
flows <- list_hvd_dataflows()
print(flows)

# List dataflows using the v2 API
flows_v2 <- list_hvd_dataflows(api_version = "hvd_v2")
}
}
