% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvd_config.R
\name{build_hvd_v1_url}
\alias{build_hvd_v1_url}
\title{Build HVD v1 (SDMX 2.1) URL}
\usage{
build_hvd_v1_url(
  endpoint,
  dataset_id = NULL,
  filter = "ALL",
  provider = "all",
  start_time = NULL,
  end_time = NULL,
  updated_after = NULL,
  lastNObservations = NULL,
  detail = NULL,
  includeHistory = NULL,
  method = "GET"
)
}
\arguments{
\item{endpoint}{Character string specifying the endpoint type. One of
\code{"data"}, \code{"availableconstraint"}, \code{"structure"}, or \code{"dataflow"}.}

\item{dataset_id}{Character string specifying the ISTAT dataset (flow)
identifier (e.g., \code{"150_908"}). Required for \code{"data"},
\code{"availableconstraint"}, and \code{"structure"} endpoints.}

\item{filter}{Character string with the SDMX positional key filter.
Default \code{"ALL"} (no filtering). Ignored when \code{method = "POST"}.}

\item{provider}{Character string identifying the data provider. Default
\code{"all"}.}

\item{start_time}{Character string specifying the start period for data
retrieval (e.g., \code{"2020"}, \code{"2020-Q1"}, \code{"2020-01"}).}

\item{end_time}{Character string specifying the end period for data
retrieval.}

\item{updated_after}{Character string in ISO 8601 format
(e.g., \code{"2025-01-01T00:00:00Z"}). When provided, the server returns only
observations updated after this timestamp.}

\item{lastNObservations}{Integer. Limits the response to the last N
observations per time series.}

\item{detail}{Character string controlling the amount of information
returned (e.g., \code{"full"}, \code{"dataonly"}, \code{"nodata"}).}

\item{includeHistory}{Logical. When \code{TRUE}, historical revisions of data
points are included.}

\item{method}{Character string, either \code{"GET"} (default) or \code{"POST"}.
Controls the URL shape for the \code{data} endpoint.}
}
\value{
Character string containing the constructed URL.
}
\description{
Constructs request URLs for the ISTAT HVD v1 API surface, which follows the
SDMX 2.1 RESTful specification. Supports the \code{data}, \code{availableconstraint},
\code{structure}, and \code{dataflow} endpoints. For the \code{data} endpoint, both GET and
POST URL shapes are available (POST places the filter key in the request body
rather than in the URL path).
}
\examples{
\dontrun{
# GET data URL with time filter
build_hvd_v1_url("data", dataset_id = "150_908",
                 start_time = "2020", end_time = "2024")

# POST data URL (filter key sent in body, not in URL)
build_hvd_v1_url("data", dataset_id = "150_908", method = "POST")

# Available constraint URL
build_hvd_v1_url("availableconstraint", dataset_id = "150_908")

# Dataflow listing URL (no dataset_id needed)
build_hvd_v1_url("dataflow")
}

}
\keyword{internal}
