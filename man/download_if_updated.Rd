% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_tracking.R
\name{download_if_updated}
\alias{download_if_updated}
\title{Download Dataset if Updated}
\usage{
download_if_updated(
  dataset_id,
  filter = NULL,
  start_time = "",
  force = FALSE,
  timestamp_file = NULL,
  timeout = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dataset_id}{Character string specifying the dataset ID}

\item{filter}{Character string specifying data filters. Default uses config value}

\item{start_time}{Character string specifying start period. Default is empty (all data)}

\item{force}{Logical indicating whether to skip update check and force download.
Default is FALSE}

\item{timestamp_file}{Character string specifying path to timestamp registry file.
If NULL, uses default from configuration}

\item{timeout}{Numeric timeout in seconds for the download. If NULL, uses config value}

\item{verbose}{Logical indicating whether to print status messages. Default is TRUE}
}
\value{
A data.table containing the downloaded data if updates available,
or NULL if no updates and force = FALSE
}
\description{
Wrapper function that checks for updates and downloads only if updates are available.
This is the main user-facing function for update-aware downloads.
}
\examples{
\dontrun{
# Download only if updated
data <- download_if_updated("534_50", start_time = "2020")

# Force download regardless of update status
data <- download_if_updated("534_50", force = TRUE)

# Download with custom filter
data <- download_if_updated("534_50", filter = "M..", start_time = "2020")
}
}
