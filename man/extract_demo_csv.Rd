% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_cache.R
\name{extract_demo_csv}
\alias{extract_demo_csv}
\title{Extract CSV Data from a Demo ZIP Archive}
\usage{
extract_demo_csv(zip_path, verbose = TRUE)
}
\arguments{
\item{zip_path}{Character string with the path to the ZIP file.}

\item{verbose}{Logical whether to log status messages. Default
\code{TRUE}.}
}
\value{
A \code{data.table} with the contents of the CSV file(s).
}
\description{
Extracts CSV files from a ZIP archive downloaded from demo.istat.it
and returns the contents as a \code{data.table}. When the archive
contains multiple CSV files they are combined with
\code{data.table::rbindlist()}.
}
\details{
Encoding is attempted first as UTF-8; if that fails (common with
older ISTAT files), Latin-1 is used as fallback.
}
\examples{
\dontrun{
dt <- extract_demo_csv("demo_data/d7b/D7B2024.csv.zip")
str(dt)
}

}
\keyword{internal}
